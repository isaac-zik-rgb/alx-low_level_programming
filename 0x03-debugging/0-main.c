#include <stdio.h>
/**
 * Main - Testing if the function positive-or_nrgative will print
 *
 * Return: Always 0 (Success)
 */
int main(void)
{
  int n;
  n = 0;
  int  positive_or_negative(n);
return (0);
}
