#include <stdio.h>
#include "main.h"
/**
 * _strcpy - copies an array character to another
 * @
 * @
 * Return: Always 0 success
 */
char *_strcpy(char *dest, char *src)
{
  int i;
  for (i = 0; scr[i] != '\0'; i++)
    {
      dest[i] = scr[i];
    }
  dest[i++] = '\0';
  return(dest);
}
