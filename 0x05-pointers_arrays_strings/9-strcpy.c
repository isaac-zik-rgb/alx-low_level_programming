#include <stdio.h>
#include "main.h"
/**
 * _strcpy - copies an array character to another
 * @
 * @
 * Return: Always 0 success
 */
char *_strcpy(char *dest, char *src)
{
  int i;
  for (i = 0; i != '\0'; i++)
    {
      dest[i] = scr[i];
      return (dest);
    }
}
