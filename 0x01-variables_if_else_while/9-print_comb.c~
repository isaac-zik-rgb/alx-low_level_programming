#include <stdio.h>
/**
 * main - Entry point
 *
 * Return: 0 (Success)
 */
int main(void)
{
int a;
int b;
int j;
b = 44;
j = 32;
for (a = 48; a <= 57; ++a)
putchar(a);
{
putchar(b);
putchar(j);
}
putchar(10);
return (0);
}
