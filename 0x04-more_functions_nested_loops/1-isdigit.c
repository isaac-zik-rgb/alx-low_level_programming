#include <stdio.h>
#include "main.h"
/**
 * _isdigit - Checks for digit number
 * @c: returns an integer value
 *
 * Return: Always 0 (success)
 */
int _isdigit(int c)
{
return (c >= 48 && c <= 57);
}
