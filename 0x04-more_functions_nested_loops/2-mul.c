#include <stdio.h>
#include "main.h"
/**
 * mul - A function tha multiplies two integer numbers
 * @a: returns an integer value in first parameter
 * @b: returns an integer as a second parameter
 * Return: Always 0 (Success)
 */
int mul(int a, int b)
{
int result;
result = a * b;
return (result);
}
